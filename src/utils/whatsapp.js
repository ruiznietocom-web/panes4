// ⚠️ ARCHIVO NO UTILIZADO (código heredado).
// La generación del mensaje de WhatsApp vive en src/components/OrderSummary.js,
// que usa los datos de src/data/products.js como única fuente de verdad.
// Se conserva este archivo solo como referencia histórica; ningún componente lo importa.
// Si en el futuro quieres reutilizar un generador de mensajes compartido,
// extrae la lógica de OrderSummary.js aquí e importa `optionalExtras`
// desde '../data/products' para no duplicar precios.

export {};
